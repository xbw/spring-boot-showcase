yieldUnescaped '<!DOCTYPE html>'
html(lang:'en') {
    head {
        meta('charset':"utf-8")
        title('Spring Boot Groovy')
    }
    body {
        p('Date: '+time)
        p('Hello, '+name+' !')
    }
}
