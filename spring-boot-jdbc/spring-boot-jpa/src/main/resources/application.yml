#debug: true
spring:
  profiles:
    active: h2
  jpa:
    hibernate:
      ddl-auto: none # This is actually a shortcut for the "hibernate.hbm2ddl.auto" property.
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        hbm2ddl:
#          auto: create
#        format_sql: true